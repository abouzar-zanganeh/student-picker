# **📚 معرفی برنامه دستیار هوشمند معلم (Smart Teacher's Assistant)**

یک ابزار تحت وب، مدرن و هوشمند برای کمک به معلمان در مدیریت کلاس، ثبت دقیق فعالیت‌ها و انتخاب عادلانه دانش‌آموزان برای مشارکت در کلاس. این برنامه به‌صورت کاملاً سمت کاربر (Client-Side) طراحی شده و با استفاده از تکنولوژی‌های پیشرفته وب (PWA, IndexedDB, Web Workers)، کارایی و امنیت داده‌ها را تضمین می‌کند.

## **🎯 فلسفه اصلی برنامه**

هدف اصلی این دستیار، حل سه چالش بزرگ در کلاس درس است:

* **مشارکت عادلانه:** اطمینان از شانس برابر برای تمام دانش‌آموزان.
* **عملکرد بدون مکث:** رابط کاربری سریع و روان حتی با حجم داده بالا.
* **امنیت داده‌ها:** سیستم پشتیبان‌گیری چندلایه و "ماشین زمان" داخلی.

---

## **🚀 ویژگی‌های کلیدی و جدید (نسخه ۱۱.۷.۰+)**

### **۱. معماری عملکرد و ذخیره‌سازی (Performance & Storage V2)**

این برنامه اکنون از یک معماری هیبریدی و حرفه‌ای برای مدیریت داده‌ها استفاده می‌کند:

* **فشرده‌سازی پس‌زمینه (Web Workers):** تمام عملیات فشرده‌سازی داده‌ها (LZ-String) در یک رشته پردازشی جداگانه (Worker) انجام می‌شود. این یعنی رابط کاربری (دکمه‌ها و انیمیشن‌ها) حتی هنگام ذخیره حجم زیادی از اطلاعات، **۱۰۰٪ روان و بدون لگ** باقی می‌ماند.
* **تأخیر هوشمند (Smart Debouncing):** ذخیره‌سازی داده‌ها با تأخیر ۵۰۰ میلی‌ثانیه‌ای انجام می‌شود تا از "فشار بی‌هوده" به پردازنده هنگام کلیک‌های سریع جلوگیری شود.
* **گاراژ داده‌ها (IndexedDB):** علاوه بر حافظه سریع (LocalStorage) برای داده‌های جاری، از یک پایگاه داده حجیم داخلی (IndexedDB) برای نگهداری فایل‌های سنگین پشتیبان استفاده می‌شود.

### **۲. سیستم «نقاط بازگشت» (Restore Points)**

یک سیستم "ماشین زمان" داخلی که امنیت داده‌های شما را تضمین می‌کند:

* **ذخیره خودکار:** هر بار که یک فایل پشتیبان دانلود می‌کنید، یک کپی از آن به‌صورت خودکار و فشرده در «گاراژ» مرورگر ذخیره می‌شود.
* **دسترسی سریع:** منوی **«↺ نقاط بازگشت»** به شما اجازه می‌دهد ۳ پشتیبان آخر خود را مشاهده و بدون نیاز به آپلود فایل، آن‌ها را بازیابی کنید.
* **چرخش خودکار:** سیستم به‌صورت هوشمند تنها ۳ نسخه آخر را نگه می‌دارد تا فضای دستگاه اشغال نشود.

### **۳. رابط کاربری پیشرفته حضور و غیاب (Attendance Card Layout)**

تب حضور و غیاب بازطراحی شده است تا روی موبایل و دسکتاپ بهترین تجربه را ارائه دهد:

* **طراحی کارت (Card Layout):** در موبایل، اطلاعات هر دانش‌آموز در ۳ ردیف مجزا و خوانا نمایش داده می‌شود تا از کلیک‌های اشتباه جلوگیری شود. در دسکتاپ، این کارت‌ها به‌صورت هوشمند به یک ردیف فشرده تبدیل می‌شوند.
* **حالت انتخاب (Selection Mode):** چک‌باکس‌ها به‌صورت پیش‌فرض مخفی هستند. با **لمس طولانی (Long Press)** روی نام دانش‌آموز، حالت انتخاب فعال شده و چک‌باکس‌ها ظاهر می‌شوند.
* **عملیات گروهی با لمس طولانی:**
    * **دکمه حضور:** لمس طولانی برای تغییر وضعیت حضور **تمام** دانش‌آموزان (مثلاً همه حاضر).
    * **دکمه تکلیف:** لمس طولانی برای تغییر وضعیت تکلیف **تمام** دانش‌آموزان.
    * **دکمه یادداشت:** لمس طولانی برای ثبت یادداشت گروهی برای کل کلاس.

### **۴. بازیابی هوشمند (Smart Restore)**

سیستم بازیابی فایل پشتیبان اکنون بسیار هوشمندتر عمل می‌کند:

* **شناسایی با ID:** کلاس‌ها بر اساس شناسه یکتا (ID) شناسایی می‌شوند، نه نام. اگر نام کلاسی را تغییر داده باشید، سیستم متوجه می‌شود و داده‌ها را به‌جای کپی کردن، به‌روزرسانی می‌کند.
* **حالت پاک‌سازی (Clean Restore):** گزینه‌ای اختیاری برای جایگزینی کامل اطلاعات فعلی با فایل پشتیبان (حذف داده‌های اضافه).
* **نمایش جزئیات:** قبل از بازیابی، لیست دقیق کلاس‌های موجود در فایل پشتیبان به کاربر نمایش داده می‌شود.

### **۵. تقویم و تاریخ بومی (Native Persian Calendar)**

* **انتخابگر تاریخ شمسی:** برای تغییر تاریخ جلسات (مثلاً ثبت جلسه جبرانی برای روزهای گذشته)، از یک سیستم انتخابگر ۳-ستونی (روز/ماه/سال) کاملاً شمسی استفاده می‌شود که مشکلات تقویم میلادی گوشی‌ها را حل می‌کند.
* **تفکیک زمان:** تاریخ نمایشی (Journal Date) از تاریخ ثبت سیستمی (Audit Date) جدا شده است تا سوابق دقیق بمانند.

---

## **🛠️ ویژگی‌های پایه**

### **انتخابگر هوشمند و منصفانه**
* الگوریتم چندمرحله‌ای برای انتخاب دانش‌آموزانی که کمترین مشارکت را داشته‌اند.
* حل تعارض با بررسی سوابق در سایر دسته‌بندی‌ها و در نهایت انتخاب تصادفی.

### **داشبورد و آمار**
* **خروج (Exit):** دکمه‌ای برای ثبت خروج موقت دانش‌آموز (بدون کسر انضباط، اما ثبت فرصت از دست رفته).
* **تولتیپ‌های لمسی:** نمایش توضیحات نمرات با لمس اعداد در پنل جزئیات.
* **هایلایت کلاس فعال:** کلاس‌هایی که در زمان جاری برگزار می‌شوند، با رنگ سبز متمایز می‌شوند.

### **مدیریت کلاس**
* پشتیبانی از کلاس‌های حضوری و آنلاین.
* افزودن دانش‌آموز از طریق لیست اکسل (CSV) یا کپی/پیست.
* **سطل زباله (Trash Bin):** امکان بازیابی تمام آیتم‌های حذف شده (دانش‌آموز، جلسه، نمره، کلاس).

### **محافظ صفحه (Screen Saver)**
* جلوگیری از سوختگی پیکسل (Burn-in) در نمایشگرهای AMOLED با نمایش ساعت و لوگوی شناور در پس‌زمینه مشکی مطلق.

---

## **💻 تکنولوژی‌های زیرساخت**

* **Vite:** بیلد سیستم فوق سریع.
* **Web Workers:** پردازش موازی برای جلوگیری از فریز شدن UI.
* **IndexedDB:** پایگاه داده NoSQL داخلی مرورگر برای ذخیره فایل‌های حجیم.
* **LZ-String:** الگوریتم فشرده‌سازی متن برای بهینه‌سازی LocalStorage.
* **Jalaali-JS:** کتابخانه دقیق تبدیل تاریخ شمسی.
* **JSZip:** مدیریت فایل‌های فشرده برای پشتیبان‌گیری.

## **📂 نحوه استفاده و توسعه**

1.  **نصب:** `npm install`
2.  **اجرا (Dev):** `npm run dev` (سرور توسعه با قابلیت‌های PWA).
3.  **بیلد:** `npm run build`

## **📄 لایسنس**
MIT License.