# **📚 معرفی برنامه دستیار هوشمند معلم (Smart Teacher's Assistant)**

یک ابزار تحت وب، مدرن و هوشمند برای کمک به معلمان در مدیریت کلاس، ثبت دقیق فعالیت‌ها و انتخاب عادلانه دانش‌آموزان برای مشارکت در کلاس. این برنامه به‌صورت کاملاً سمت کاربر (Client-Side) و با جاوااسکریپت خالص (Vanilla JS) نوشته شده و تمام داده‌ها را به‌صورت امن در حافظه مرورگر شما (localStorage) ذخیره می‌کند.

## **🎯 فلسفه اصلی برنامه**

هدف اصلی این دستیار، حل دو چالش بزرگ در کلاس درس است:

* **مشارکت عادلانه:** اطمینان از اینکه تمام دانش‌آموزان به یک اندازه شانس مشارکت در فعالیت‌های کلاسی را داشته باشند.
* **ردگیری دقیق عملکرد:** علاوه بر ثبت نمرات، ثبت داده‌های آماری دقیق و کاربردی از عملکرد هر دانش‌آموز در طول ترم.

## **🚀 ویژگی‌های کلیدی**

### **۱. انتخابگر هوشمند و منصفانه (الگوریتم چندسطحی)**

این برنامه از یک الگوریتم چندمرحله‌ای دقیق استفاده می‌کند تا عدالت آموزشی را به حداکثر برساند:

* **فیلتر اولیه (بر اساس دسته‌بندی فعلی):**
    * سیستم کمترین تعداد انتخاب را در دسته‌بندی فعال (مثلاً «گرامر») شناسایی می‌کند.
    * سپس، لیستی از دانش‌آموزانی که با این تعداد انتخاب برابر هستند تهیه می‌کند.
* **اولین گره‌گشا (بر اساس سایر دسته‌بندی‌ها):**
    * اگر تنها یک نفر کاندیدا باشد، او انتخاب می‌شود.
    * در صورت وجود چند کاندیدا، سیستم مجموع انتخاب‌های آن‌ها را در *سایر دسته‌بندی‌ها* محاسبه می‌کند.
    * دانش‌آموزی که مجموع کمتری دارد، انتخاب می‌شود.
* **گره‌گشای نهایی (انتخاب تصادفی):**
    * اگر شرایط بین چند نفر کاملاً برابر باشد، انتخاب نهایی به‌صورت تصادفی انجام می‌شود.

### **۲. داشبورد جلسه (Session Dashboard)**

صفحه مدیریت جلسه به دو تب اصلی تقسیم شده است که در موبایل با **کشیدن انگشت (Swipe)** می‌توان بین آن‌ها جابجا شد:

* **تب انتخابگر (Selector):** دسترسی به جدول آمار، انتخاب دانش‌آموز و ثبت نمره.
* **تب حضور و غیاب (Attendance):** مدیریت وضعیت حضور و تکالیف.

### **۳. جدول آمار تعاملی (Interactive Statistics Table)**

جدول آمار یک هاب تعاملی برای نظارت و نمره‌دهی سریع است:

* **نمایش نمرات:** نمایش نمرات واقعی هر دانش‌آموز در دسته‌بندی‌های نمره‌دهی.
* **ستون نمره نهایی (Final Score):** محاسبه خودکار میانگین وزنی مهارت‌ها (Reading, Writing, Listening, Speaking) برای ارائه نمره نهایی کلاس.
* **یادداشت‌های شناور (Tooltips):** نمایش توضیحات نمرات با نگه داشتن ماوس.
* **انتخاب مستقیم:** کلیک روی سلول‌های جدول برای انتخاب دستی دانش‌آموز در آن دسته‌بندی.

### **۴. مدیریت پیشرفته وضعیت و تکالیف**

* **مدیریت تکالیف:**
    * کنترل سه‌وضعیتی (کامل ✅، ناقص ⚠️، انجام نشده ❌).
    * **ثبت یادداشت گروهی:** امکان انتخاب چند دانش‌آموز و ثبت یک یادداشت تکلیف مشترک برای همه آن‌ها با یک کلیک.
* **مدیریت حضور:**
    * ثبت غیبت، تأخیر و خروج موقت.
    * دکمه **خروج (Exit):** برای دانش‌آموزانی که موقتاً در کلاس نیستند (بدون کسر نمره انضباطی، اما ثبت به عنوان فرصت از دست رفته).
    * کپی سریع لیست غایبین برای ارسال در گروه‌های پیام‌رسان.

### **۵. محافظ صفحه هوشمند (Smart Screen Saver)**

برای جلوگیری از مصرف باتری و آسیب به نمایشگر (Burn-in) در دستگاه‌های موبایل، سیستم محافظ صفحه تعبیه شده است:

* **فعال‌سازی خودکار:** پس از ۲ دقیقه بی‌تحرکی.
* **نمایش اطلاعات:** نمایش ساعت، نسخه برنامه و لوگوی متحرک در پس‌زمینه مشکی مطلق (AMOLED friendly).
* **غیرفعال‌سازی:** با لمس صفحه یا تکان دادن ماوس.

### **۶. مدیریت کلاس و دانش‌آموزان**

* ایجاد کلاس‌های متعدد (حضوری / آنلاین).
* افزودن دانش‌آموز از طریق: دستی، کپی/پیست لیست، و فایل CSV.
* **انتقال دانش‌آموز:** جابجایی دانش‌آموز بین کلاس‌ها با حفظ تاریخچه نمرات و ایجاد یادداشت سیستمی خودکار در کلاس مقصد.
* **سطل زباله (Trash Bin):** بازیابی کلاس‌ها، دانش‌آموزان، جلسات و نمرات حذف شده.

### **۷. پروفایل دانش‌آموز و سیستم نمره‌دهی**

* **تاریخچه کامل:** نمایش تمام نمرات، یادداشت‌ها و سوابق انضباطی.
* **ویرایش سریع:** قابلیت کلیک روی متن یادداشت‌ها یا نمرات برای ویرایش مستقیم (Click-to-Edit).
* **خلاصه وضعیت:** نمایش تعداد کل انتخاب‌ها، غیبت‌ها و فرصت‌های از دست رفته.

### **۸. پشتیبان‌گیری و بازیابی پیشرفته (نسخه ۲.۰)**

* **پشتیبان‌گیری (Backup):**
    * استفاده از فرمت فشرده (Zip/Base64) برای کاهش حجم و حفظ سلامت داده‌های فارسی.
    * نام‌گذاری خودکار فایل بر اساس تاریخ و ساعت شمسی.
    * اشتراک‌گذاری مستقیم فایل از طریق Web Share API در موبایل.
* **بازیابی (Restore):**
    * **حالت افزودن (Append):** اضافه کردن کلاس‌های فایل پشتیبان به کلاس‌های فعلی (مناسب برای ادغام داده‌ها).
    * **حالت جایگزینی (Overwrite):** جایگزینی کامل اطلاعات فعلی با فایل پشتیبان.
    * تشخیص هوشمند نسخه فایل پشتیبان.

### **۹. جستجوی سریع**

* **جستجوی سراسری:** یافتن دانش‌آموز یا کلاس در کل برنامه از طریق نوار جستجوی صفحه اصلی.
* **جستجوی محلی:** فیلتر کردن لیست دانش‌آموزان در داخل کلاس یا لیست حضور و غیاب.
* پشتیبانی از اصلاح حروف فارسی/عربی (مانند ی/ی و ک/ک) و نگاشت کیبورد (تایپ فارسی با کیبورد انگلیسی).

### **۱۰. فرم نمره‌دهی سریع (Quick-Grade)**

یک ابزار همیشه در دسترس در صفحه داشبورد برای ثبت نمره و یادداشت بدون نیاز به باز کردن پروفایل کامل دانش‌آموز. ورودی‌ها پس از ثبت به‌صورت خودکار پاک می‌شوند.

### **۱۱. فرایند هوشمند همسان‌سازی (Smart Onboarding)**

هنگام افزودن دانش‌آموز جدید به کلاسی که جلسات آن شروع شده است:
* **شبیه‌سازی مشارکت:** آمار مشارکت (تعداد انتخاب‌ها) بر اساس فعال‌ترین دانش‌آموز کلاس شبیه‌سازی می‌شود تا دانش‌آموز جدید شانس برابر داشته باشد.
* **محاسبه نرخ غیبت:** میانگین نرخ "فرصت‌های از دست رفته" کلاس برای او محاسبه و اعمال می‌شود.
* **ثبت غیبت‌های گذشته:** برای تمام جلسات قبلی کلاس، وضعیت "غایب" ثبت می‌شود.
* **یادداشت خودکار:** سیستم یک یادداشت توضیحی دقیق در پروفایل او ثبت می‌کند.

### **۱۲. قابلیت‌های مخفی (Easter Eggs)**

* **ساخت کلاس تستی:** ۵ کلیک سریع روی عنوان «لیست کلاس‌ها».
* **صفر کردن آمارها:** ۵ کلیک سریع روی عنوان «آمار عملکرد» در داشبورد.

## **🛠️ تکنولوژی‌های استفاده‌شده**

* **Vite:** ابزار ساخت (Build Tool) و سرور توسعه.
* **JavaScript (ES6+):** هسته اصلی با معماری ماژولار.
* **JSZip:** برای فشرده‌سازی و مدیریت فایل‌های پشتیبان.
* **PWA:** قابلیت نصب روی موبایل و دسکتاپ و کارکرد آفلاین.
* **LocalStorage:** پایگاه داده محلی مرورگر.

## **📂 نحوه استفاده**

1.  **نصب وابستگی‌ها:** `npm install`
2.  **اجرا در حالت توسعه:** `npm run dev` (تولید خودکار گواهی SSL برای تست ویژگی‌های PWA).
3.  **ساخت نسخه نهایی:** `npm run build`

## **🚀 استقرار و به‌روزرسانی**

پروژه از GitHub Actions برای استقرار خودکار روی GitHub Pages استفاده می‌کند. هر تغییر در شاخه `main` به‌صورت خودکار بیلد و منتشر می‌شود.

## **📄 لایسنس**
MIT License.